__pycache__
.claude
.vscode
.venv
.vs
data
build
model
mapgen
checkpoints
checkpoints2
*.npz
*.exe
*.wasm
extern
vcpkg-overlays
vendor
.clang-format

# emscripten out-of-source build (fetches and builds raylib, ~500MB)
build-web/

# the wasm rule above is for build artifacts; the demo bundles actually served
# by nginx have to be committed
!site/demos/**/*.wasm
