services:
  site:
    build:
      context: ./site
    depends_on:
      - runner

  runner:
    build:
      context: .            # repo root — needed to COPY projects/
      dockerfile: runner/Dockerfile
    expose:
      - "8000"
